#include <stdio.h>

int main()
{
    int t;
    scanf("%d",&t);

    while(t--)
    {
        long long a,b,gcd = 0;
        scanf("%lld %lld",&a,&b);
       // printf("%lld",gcd(a,b));
   //     printf("%s\n",isSumofDigitsPrime(gcd(a,b)));
    }

}